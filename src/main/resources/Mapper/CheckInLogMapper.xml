<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.treehole.mapper.CheckInLogMapper">

    <insert id="insert" parameterType="org.example.treehole.entry.CheckInLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO check_in_log (user_id, check_in_date, create_time)
        VALUES (#{userId}, #{checkInDate}, NOW())
    </insert>

    <select id="selectCheckInDatesByMonth" resultType="java.lang.String">
        SELECT DATE_FORMAT(check_in_date, '%Y-%m-%d')
        FROM check_in_log
        WHERE user_id = #{userId}
          AND DATE_FORMAT(check_in_date, '%Y-%m') = #{monthPrefix}
    </select>

</mapper>
